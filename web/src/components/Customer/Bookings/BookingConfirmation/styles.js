import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
	items: {
		justifyContent: 'center',
		justifyItems: 'center',
		alignItems: 'center',
		alignContent: 'center',
	},
});
